server:
  port: 0

eureka:
  instance:
    hostname: localhost
    instance-id: ${eureka.application.name}:${random.uuid}
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

tmpVar: 100
